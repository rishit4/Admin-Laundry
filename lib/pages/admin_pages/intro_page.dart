import 'package:flutter/material.dart';

class UserListPage extends StatelessWidget {
  static const String id = "User-List";

  const UserListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('User Lists'),
    );
  }
}
