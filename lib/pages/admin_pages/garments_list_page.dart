import 'package:flutter/material.dart';

class GarmentsListPage extends StatelessWidget {
  static const String id = "Garments-Lists";

  const GarmentsListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Garments Lists'),
    );
  }
}
